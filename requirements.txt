# Portable indexing and analysis environment (Python 3.10+ recommended)
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
opencv-python-headless>=4.8
Pillow>=10.0
openpyxl>=3.1
tqdm>=4.66
albumentations>=1.4
matplotlib>=3.8
seaborn>=0.13
torch>=2.4
torchvision>=0.19
timm>=1.0
thop>=0.1.1

# Training also requires CUDA-matched builds of mamba-ssm and causal-conv1d.
# Follow the setup cell in the training notebooks rather than installing them here.
